# Coherence Gate — term sheet vs. booking, checked before it costs money

**The term sheet is the desk's intent. The booking record is the bank's truth.
This agent closes the gap between them before a confirmation goes out wrong.**

## Problem

Document-to-booking coherence is checked by hand today, and the hours scale linearly with
strategy and trade count. The dangerous failure is silent: a barrier level, a day count, a
memory-coupon clause that differs between the document and the system, invisible until
reconciliation, a confirmation, or a coupon payment surfaces it. I have paid for this
failure class personally: a seven-figure loss on an order that silently never reached the
execution tool. This is the system I wish had existed upstream of it.

## What it does

```
term_sheet.txt ──► Extractor A (Gemini 3.8 Flash) ──┐
               ──► Extractor B (Claude Opus 5)    ──┴─► guard ─► normalize ─► merge      (code)
                                                                                 │
booking store ─► MCP tool booking_lookup(trade_id) ───────────────────► comparator (code)
                                                                                 │
                                                                    findings, typed per field
                                                              ┌──────────────────┴────────────┐
                                                       AUTO_CLEAR lane                   TRIAGE agent (LLM)
                                                       agree ∧ pass → log only           drafts the desk query
```

Two model families independently extract the term sheet into a versioned 20-field schema;
every field is either cited to a verbatim source span (offsets recomputed in code) or
explicitly declared absent. Deterministic code normalizes both extractions, checks that the
families agree, fetches the booking record through an MCP tool, and compares under an exact
tolerance table. Where both families agree and the comparison passes, the field auto-clears
with zero human touch. Anything else, a mismatch, a family disagreement, a declared absence,
becomes a typed finding, and an LLM drafts the desk query citing the exact clause and booking
field.

**Models extract and explain; code decides.** No LLM ever makes the match/no-match call,
applies a tolerance, normalizes a value, or arbitrates between the two families.
Autonomy is earned per field, not granted globally: the review queue exists by design.

## Results (12 synthetic documents: 9 planted discrepancies, 1 normalizer trap, 2 clean controls)

| Metric | Result |
|---|---|
| Planted-discrepancy catch rate (right field AND right type) | **9/9** |
| False flags on clean fields | **0/218** |
| Clean documents auto-cleared with zero human touch | **2/2** |
| Auto-clear correctness (nothing planted was auto-cleared) | **9/9 (100%)** |
| Cross-family extraction agreement (fields) | 228/228 |
| Normalizer trap (2.0625% per quarter vs 8.25% p.a.) resolved without a flag | 1/1 |
| Cost per document (both extractions, traced tokens × list prices) | $0.113 |

<sub>Run 20260911-223332; generated by `scripts/fill_brief.py`, never edited by hand.</sub>

The planted set includes the hard cases: a rate quoted per-quarter in the document and
per-annum in the booking (resolved by deterministic normalization, not flagged), and a
document that genuinely omits its barrier level (produces a critical `TS_ABSENT` finding,
never a blank).

**Effort sweep, measured (eval_log.md):** with Gemini's thinking set to low instead of medium,
cost per document fell 37% and Gemini's median latency fell from 27 s to 3 s, at the price of
one mis-mapped field (8/9). Medium ships; the trade-off is documented, not hidden.

**Honest ceiling:** n = 12 documents, directional, not statistically significant. Synthetic
documents generated from parameters across four layout families; one prompt per extractor;
one run per number, no retries. The eval cannot see error classes it does not plant. Full
iteration history, including the aborted first run and every normalizer fix, is in
`eval_log.md`.

## Why this architecture survives a bank

Independent implementations that must agree before a result is trusted, the same principle
as external replication of an index level, applied to extraction. Every model call is traced
(model, version, tokens, latency, cost); every run is reproducible; prompts, schema,
tolerances and the golden set are versioned, so a model deprecation is a rerun and a diff,
not a quarter of rework. Both model families are first-class in Vertex Model Garden, the
pipeline is a sequence of tool-shaped steps built on ADK patterns with books-and-records
reached only through MCP, so the whole thing runs inside an existing Google Cloud governance
posture unchanged.

## Where this goes

The gate pattern, golden set first, dual-family extraction, deterministic decision,
eval-gated autonomy, extends directly to reconciliation break triage and P&L attribution
commentary: the same judgment-heavy hours that grow with every strategy onboarded. The
measure of success is the one you named: the marginal cost of the next strategy falls, and
you don't hear about it.

— Sheldon Gomes · github.com/sheldongomes1/coherence-gate
